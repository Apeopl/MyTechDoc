一、Lambda表达式
二、Stream(流)
	1、Collection主要是为了存储和访问数据，而Stream主要用于描述对数据的计算。
三、函数式编程
	1、函数式接口：只定义一个抽象方法的接口
	2、Lambda表达式允许直接以内联的形式为函数式接口的抽象方法提供实现，并把整个表达式作为函数式接口的实现。
四、行为参数化
	1、一个方法接受多个不同的行为作为参数，并在内部使用它们，完成不同行为的能力。
五、Optional<T>类
	1、是一个容器对象，避免出现NullPoint异常。可以包含也可以不包含一个值。
六、模式匹配